// import { socialMediaDomainConst } from '../../domain/const/social-media-domain.const';
// import { SocialMediaBuilderModel } from '../model/builder/social-media-builder.model';

// export class SocialMediaBuilder {
//   static buildData(): SocialMediaBuilderModel {
//     return Object.values(socialMediaDomainConst)
//       .sort((prev, next) => prev.metadata.order - next.metadata.order)
//       .map((sectionDomain) => ({ ...sectionDomain, extend: {} }));
//   }
// }
